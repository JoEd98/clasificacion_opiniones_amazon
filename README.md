
## Instalación de dependencias
```
pip install transformers datasets accelerate peft scikit-learn seaborn
pip install numpy
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
```